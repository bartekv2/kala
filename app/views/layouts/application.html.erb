<!DOCTYPE html>
<html>
  <head>
    <title>Kala</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body class="min-h-screen bg-gray-200">

    <header class="bg-teal-500 text-white pt-6 md:py-6">
      <div class="container flex justify-around md:items-center mx-auto px-4 flex-wrap">
        <label for="menu-toggle" class="cursor-pointer md:hidden block">
          <svg class="fill-current text-white hover:text-gray-400" xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 20 20"><title>menu</title><path d="M0 3h20v2H0V3zm0 6h20v2H0V9zm0 6h20v2H0v-2z"></path></svg>
        </label>
        <input class="hidden" type="checkbox" id="menu-toggle" />
          <nav class="hidden md:flex text-base md:items-center text-base md:w-auto w-full" id="menu">
            <ul class="md:flex items-center justify-between uppercase">
              <li class="<% if params[:controller] == "home" && params[:action] == "index" %>active <% end %>border-t md:border-0 md:ml-4">
                <a class="block md:inline hover:text-gray-400 py-4 md:py-0" href="<%= root_path %>">home</a>
              </li>
              <li class="<% if params[:controller] == "home" && params[:action] == "about" %>active <% end %>border-t md:border-0 md:ml-4">
                <a class="block md:inline hover:text-gray-400 py-4 md:py-0" href="<%= about_path %>">o mnie</a>
              </li>
              <li class="<% if params[:controller] == "home" && params[:action] == "body" %>active <% end %>border-t md:border-0 md:ml-4">
                <a class="block md:inline hover:text-gray-400 py-4 md:py-0" href="<%= body_path %>">praca z ciałem</a>
              </li>
              <li class="<% if params[:controller] == "home" && params[:action] == "coaching" %>active <% end %>border-t md:border-0 md:ml-4">
                <a class="block md:inline hover:text-gray-400 py-4 md:py-0" href="<%= coaching_path %>">poradnictwo psychologiczne / coaching</a>
              </li>
              <li class="<% if params[:controller] == "home" && params[:action] == "workshops" %>active <% end %>border-t md:border-0 md:ml-4">
                <a class="block md:inline hover:text-gray-400 py-4 md:py-0" href="<%= workshops_path %>">warsztaty</a>
              </li>
              <li class="<% if params[:controller] == "home" && params[:action] == "contact" %>active <% end %>border-t md:border-0 md:ml-4">
                <a class="block md:inline hover:text-gray-400 py-4 md:py-0" href="<%= contact_path %>">kontakt</a>
              </li>
            </ul>
          </nav>
      </div>
    </header>

    <div class="container mx-auto px-4">
      <%= yield %>
      <footer class="text-center"><p>Copyrights KALA <%= DateTime.now.year %></p></footer>
    </div>

  </body>
</html>
